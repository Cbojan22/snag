[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "snag"
version = "0.3.1"
description = "Download videos and images from any online source at highest quality without watermarks"
requires-python = ">=3.10"
license = {text = "MIT"}

[project.scripts]
snag = "src.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
